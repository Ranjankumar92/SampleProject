---
name: Sample project build
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch: null
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository 1
        uses: actions/checkout@v4
        id: checkout1
      
      - name: Run a one-line script
        run:  echo "Hello Cats config files"
